---
title: In-Context Reward Adaptation for Robust Preference Modeling
title_zh: 上下文奖励自适应实现稳健偏好建模
authors: "Zhenyu Sun, Zheng Xu, Ermin Wei"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30323v1"
tags: ["query:llm-rl"]
score: 9.0
evidence: LLM用于RLHF中的奖励函数适应
tldr: 针对RLHF中静态奖励模型无法泛化到未见偏好领域的问题，提出In-Context Reward Adaptation框架，利用transformer的上下文学习能力，通过少量偏好演示自适应推断奖励结构。发现标准transformer存在对真实奖励的渐近偏差，但引入人类响应时间作为辅助信号后，模型能成功适应未知偏好分布。该方法为异构偏好建模和偏好分布漂移提供了更鲁棒的基础，推动了灵活的人机对齐。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30323v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 966, \"height\": 664, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30323v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30323v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 919, \"height\": 300, \"label\": \"Table\"}]"
motivation: 静态奖励模型无法泛化到多样且未知的人类偏好，现有多奖励框架局限于已知领域且需昂贵重训练。
method: 提出Transformer框架，利用上下文学习从小样本偏好演示中自适应推断奖励，并引入人类响应时间作为辅助信号。
result: 标准Transformer对真实奖励存在渐近偏差，而加入响应时间后能成功适应未见过的偏好领域。
conclusion: 该方法为异构偏好和分布漂移提供了更鲁棒的偏好建模基础，可扩展至灵活的人机对齐。
---

## 摘要
基于人类反馈的强化学习（RLHF）通常依赖静态奖励模型来使大型语言模型与人类偏好对齐。然而，人类价值本质上是多样且异质的，单一的奖励模型往往缺乏泛化到未见偏好领域所需的稳健性。尽管现有的多奖励框架试图解决这一问题，但它们通常局限于一组固定的已知领域，且无法在无需昂贵重新训练的情况下适应未见的人类分布。在这项工作中，我们提出了上下文奖励自适应（In-Context Reward Adaptation），一种基于Transformer的框架，旨在实时建模多样且未见的人类偏好。通过利用Transformer的上下文学习能力，我们的方法从少量偏好演示中自适应地推断底层奖励结构。我们证明，标准的Transformer架构对该任务而言是不足的——它具有渐近偏差到真实值的特征——而将人类响应时间作为辅助输入信号，则使模型能够成功适应来自先前未见领域的偏好。我们的研究结果表明，这种方法为偏好建模提供了更稳健的基础，能够表示异质奖励和偏好分布偏移，并为更灵活的人机对齐提供了可扩展的路径。

## Abstract
Reinforcement Learning from Human Feedback (RLHF) typically relies on static reward models to align Large Language Models with human preferences. However, human values are inherently diverse and heterogeneous, and a single reward model often lacks the robustness required to generalize to unseen preference domains. While existing multi-reward frameworks attempt to address this, they are often restricted to a fixed set of known domains and fail to adapt to unseen human distributions without costly retraining. In this work, we propose In-Context Reward Adaptation, a transformer-based framework designed to model diverse and unseen human preferences on the fly. By leveraging the in-context learning capabilities of transformers, our approach adaptively infers the underlying reward structure from a small set of preference demonstrations. We demonstrate that while a standard transformer architecture is insufficient for this task by characterizing an asymptotic bias to the ground-truth, incorporating human response time as an auxiliary input signal enables the model to successfully adapt to preferences from previously unseen domains. Our findings show that this approach provides a more robust foundation for preference modeling, allowing for the representation of heterogeneous rewards and preference distribution shift, and offering a scalable path toward more flexible human-AI alignment.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在基于人类反馈的强化学习（RLHF）中，传统的静态奖励模型假设人类偏好是同质且稳定的，无法泛化到多样且动态变化的人类偏好。现有多奖励框架虽然尝试解决异构性，但只能覆盖预先定义的有限领域，遇到未见过的偏好分布时仍需昂贵的重新训练。
- **研究动机**：探索一种无需参数更新即可在推理时自适应推断未知奖励的范式，利用Transformer的上下文学习（ICL）能力。
- **背景意义**：人类偏好天然异质且随时间变化，需要一种鲁棒、可扩展的对齐机制，以适应偏好分布漂移和未见过的用户群体。

### 2. 方法论的核心思想、关键技术细节与流程

- **核心思想**：使用Transformer进行上下文奖励自适应（In-Context Reward Adaptation）。给定新用户（未知奖励参数θ_new）的少量偏好演示，模型在推理时直接推断其奖励函数，无需重新训练。
- **关键技术细节**：
  - **偏好模型**：采用线性奖励参数化 `r_i(x,y)=φ(x,y)^Tθ_i`，偏好遵循 Bradley–Terry 模型。
  - **Transformer架构**：采用单层线性注意力Transformer，简化分析但保留核心特性。输入为偏好演示的差分特征和标签。
  - **标准ICL的不足**：理论证明（Theorem 2）仅使用二元偏好标签时，即使无限数据，在未见过的奖励参数上预测必然存在渐近偏差，因为二元标签只编码了`φ^Tθ`的符号，丢失了幅度信息，导致表示空间非线性压缩。
  - **引入响应时间（Response Time）**：受认知决策模型启发，利用漂移-扩散过程生成响应时间，其条件期望与`φ^Tθ`成反比。通过组合偏好标签和响应时间，可恢复线性信号 `θ^Tφ ∝ E[z/t]`，从而消除歧义。
  - **训练与预测**：使用平均比率 `z_i/t_i` 作为目标，以平方回归损失训练Transformer。理论证明（Theorem 3 & Corollary 1）训练后U* = Σ_φ^{-1}，使得对任意θ_new，预测 `ˆo_q → 2θ_new^T φ_q`，实现零偏差自适应。
- **流程说明**：
  1. 训练阶段：从已知偏好分布H中采样多组人类类型，为每组生成多个K个标注者的偏好和响应时间，构建增强提示矩阵（包含φ_diff、z/t）。训练Transformer最小化平方回归损失。
  2. 推理阶段：给定新用户θ_new的M个演示（每个演示由K个标注者平均得到），输入Transformer，直接输出奖励预测。

### 3. 实验设计：数据集、场景、Benchmark与对比方法

- **合成数据集**：
  - 特征维度d，φ0, φ1从固定分布采样，θ_i从两个高斯混合分布采样（训练分布），θ_new从第三个独立高斯分布采样（OOD分布）。
  - 响应时间通过模拟漂移-扩散过程生成（每查询K个独立标注者）。
  - Benchmark：在ID（同分布）和OOD（分布外）设置下，评估预测准确性。
  - 对比方法：两种架构（线性注意力Transformer、GPT-2 124M），每种都测试 **无响应时间（w/o resp）** 和 **有响应时间（w/ resp）**。

- **真实数据集：Food-Risk Preferences** (Smith & Krajbich, 2018)：
  - 包含42名参与者的二元选择与响应时间，每名参与者60-200个试次。
  - 特征：每个臂包含两个食品项目的用户评分，加上二阶多项式特征（共5维）。
  - 场景：将部分参与者作为新人类类型（OOD），其余用于训练。
  - Benchmark：改变推理长度 M（4,8,16），报告ID和OOD下的准确率。
  - 对比方法：同样对比有无响应时间。

- **消融与充分性**：
  - 合成数据：表1展示不同架构、有无响应时间、ID/OOD的测试准确率。
  - 真实数据：表2与图1展示GPT-2在不同M下ID/OOD准确率均值和标准差，并观察随M增加的趋势。
  - 理论分析验证：Theorem 1-3 与 Corollary 1 从理论上支撑实验结果。

### 4. 资源与算力

论文未明确说明实验所使用的GPU型号、数量或训练时长。仅在实验中提到使用了GPT-2模型（124M参数）和线性注意力模型，但具体计算资源信息缺失。

### 5. 实验数量与充分性

- **实验组数**：包含两项主要实验（合成+真实），每项实验内部有多种条件（架构、ID/OOD、有无响应时间、不同M）。表1有2架构×2设置×2条件=8个数字；表2有2设置×3 M=6个数字；图1展示了不同M下的均值±标准差，共6条曲线。
- **充分性**：
  - 合成实验验证了理论预测（binary-only在OOD时性能下降，加入RT后恢复）。
  - 真实实验增加了外部有效性，且结果与趋势一致。
  - 消融了推理长度M，展示了ICL能力随演示数量增加而增强。
  - 实验设计相对全面，但可考虑更多特征分布或更复杂的架构拓展。

### 6. 主要结论与发现

- **核心结论**：仅依赖二元偏好标签的上下文学习无法鲁棒适应未见的人类偏好（产生渐近偏差）。引入人类响应时间作为辅助信号，可恢复幅值信息，实现零偏差的上下文奖励自适应。
- **理论发现**：定性证明了binary-only的不足源于信息瓶颈（θ→μ(θ)是非线性压缩），而响应时间线性化了嵌入，使Transformer的线性解码器可完美逆映射。
- **实验发现**：合成数据和真实数据一致显示，加入响应时间显著提升OOD性能，缩小与ID性能的差距，且随演示数量增加效果更明显。

### 7. 优点

1. **理论分析深入**：严格证明了binary-only无法ICL、以及响应时间可解决的理论界限（Theorem 2, 3, Corollary 1）。
2. **方法新颖且实用**：利用认知科学中的响应时间作为低成本辅助信号，无需改变标注协议，易于在实际RLHF中部署。
3. **实验设计兼顾内外部效度**：合成数据可控验证理论，真实数据验证泛化性。
4. **消融充分**：包括不同架构（简单线性注意力和强大GPT-2）、不同推理长度、ID/OOD区分。
5. **结果一致性**：理论与实验高度吻合，增强了可信度。

### 8. 不足与局限

1. **架构简化**：理论分析局限于线性注意力Transformer，虽实验扩展到GPT-2，但缺乏对更复杂架构（如DeepMind的Chinchilla等）的理论保证。
2. **响应时间收集困难**：在真实大规模部署中，准确测量每个标注者的响应时间可能存在噪声或不可行；论文未讨论噪声鲁棒性。
3. **K个标注者假设**：方法依赖每个查询由K个独立标注者产生平均响应时间，这在实践中可能成本较高或延迟。
4. **实验覆盖有限**：仅在单一尺寸模型（GPT-2 124M）和单一真实数据集上验证，缺乏对更大LLM（如7B）及更多样化任务（如对话、摘要）的评估。
5. **未对比SOTA方法**：论文未与其他异构偏好建模方法（如元学习FSPO、多奖励网络Park et al.）进行直接实验对比。
6. **资源算力未披露**：不利于复现和公平比较。

（完）
